
static var a : float;

func int myFunc ()
    var b: int;
{

    return 0;
}

static func void run ()
    var b : int;
{

    b = 0;

    while (b < 10){  
        stop();
        jump();
        speak(b);
        accel(10.0f + 10.0f);
        rot(10.0f + 20 * (40+50));
        b = b+1;
    }
    

}


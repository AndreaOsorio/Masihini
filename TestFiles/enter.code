func int recursive(num:int){

    if(num==0 || num == 1){
        return num;
    }else{
        return recursive(num - 1) + recursive(num - 2);
    }
}

func float factorial(num:float){
    
    if (num == 0.0f || num==1.0f){
        return 1.0f;
    }else{
        return factorial(num - 1) * num;
    }
}

static func void run ()
    var a : int;
    var b : float;
    var c : float [5];
    var dArr : int [5][4][3];
    var e : boolean [6];
    var f : string [10];
{
    a = recursive(10);
    speak(a);

    b = factorial(3.0f);
    speak(b);

    dArr[1][3][2] = 12;
    dArr[0][2][1] = 3;

    speak(dArr[1][3][2]);
    speak(dArr[0][2][1]);

    a = dArr[1][3][2] + dArr[0][2][1];
    speak(a);


}
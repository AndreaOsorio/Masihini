func int recursive(num:int){

    if(num==0 || num == 1){
        return num;
    }else{
        return recursive(num - 1) + recursive(num - 2);
    }
}

func float factorial(num:float){
    
    if (num == 0.0f || num==1.0f){
        return 1.0f;
    }else{
        return factorial(num - 1) * num;
    }
}

func int arrayExample(arr:int[10])
    var i : int;
{
    speak('retrieve');
    i = 0;
    while(i<10){
        speak(arr[i]);
        i = i+1;
    }

    return i;

}

static func void run ()
    var a : int;
    var x : int;
    var y : int;
    var z : int;
    var b : float;
    var c : float [5][2];
    var dArr : int [5][4][3];
    var e : boolean [6][2];
    var f : string [10][2];
    var g : int [10];
{
    a = recursive(10);
    speak(a);

    b = factorial(3.0f);
    speak(b);

    a = 0;
    speak('assign');
    while(a<10){
        g[a] = a;
        speak(g[a]);
        a = a + 1;
    }
    a = arrayExample(g);
    speak(a);

}